针对C 语言的基础语法做一些测试

测试枚举变量，枚举变量的值大小是 4 个字节

枚举值是常量,默认第一个枚举常量的值是 0

Q:test_func，测试函数内部能否使用代码块作用域内的变量
A: 不能

测试 typedef，typedef的作用是给数据类型取别名


geometry 打印三角形和菱形
for循环嵌套两个for循环：
最外层for循环控制打印的行数（每行中要考虑打印的空格和 * 的个数）
第一行打印的 * 个数是 1，第二行是 3 依次类推
第一行打印的 空格 个数是 n-1/2,第二行是 n-3/2

