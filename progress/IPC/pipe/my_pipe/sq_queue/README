通过循环队列实现管道的功能,队列结构中加互斥量和条件变量，用于保证数据的一致性 避免竞争

库中尽量不要做输出，程序终止这些，通过 return 一些值，然后交给用户去判断做同一的出错处理：
比如在库中向标准输出输出错误信息，但是用户程序是在守护进程中调用了这些库，标准输出被关闭



然后计算取质数，每次放多个数


2022.10.11
当前程序实现的就是一个循环队列


管道的读写每次应该包含多少个字节，并且包括出错处理  还需要满足多个进程同时操作保证数据的一致性


！！！队列中空着的一个元素位置是一直变动的，不是固定的  这个空位是用来判断队列满
